<div class="container">
  <h2>ジャンル編集</h2>
  <div class="row">
    <div class="col">
      <%= form_with model: @genre, url: admin_genre_path, local:true do |f| %>
        <%= f.label :ジャンル %>
        <%= f.text_field :name %>
        <%= f.submit "変更を保存" ,class: "btn btn-primary" %>

      <% end %>

    </div>
  </div>
</div>